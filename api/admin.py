from django.contrib import admin
from rest_framework.authtoken.models import Token
 # Import from your app where Assure_User is defined

  # Search tokens by user's username

    # Other admin configuration options (e.g., list_display)
